name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: main
    steps:
      # @intelena/shielded-notes is consumed via file:../../../shielded-notes until it is on npm,
      # so check both repos out as siblings.
      - uses: actions/checkout@v4
        with:
          path: main
      - uses: actions/checkout@v4
        with:
          repository: intelena/shielded-notes
          path: shielded-notes
      # The repo is checked out under main/, so point the action at that package.json for the pnpm version.
      - uses: pnpm/action-setup@v4
        with:
          package_json_file: main/package.json
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Build shielded-notes
        working-directory: shielded-notes
        run: pnpm install --frozen-lockfile && pnpm build
      - run: pnpm install --frozen-lockfile
      # Build first: the CLI package typechecks against core's dist.
      - run: pnpm build
      - run: pnpm typecheck
      - run: pnpm test
